# --- BASIC TOKENIZATION & SPACING ---
ls
ls -la
   ls      -la    (veel spaties fixe)
ls|grep a
ls>file
ls>>file
ls<file

# --- REDIRECTION & PIPE BOUNDARIES ---
ls|ls|ls|ls
cat<file|grep a>out
>>file_app ls
>file_trunc ls
<<<< (Should handle as double heredoc tokens) (beter exiten)
>>>> (Should handle as double append tokens) 

# --- QUOTES & INDEXING ---
echo "hello world"
echo 'hello world'
echo "  ls  "
echo ""
echo ''
ls" -la"
"ls" -la
'ls'|'grep' a

# --- EXPANSION (ENVIRONMENT VARIABLES) ---
echo $USER
echo "$USER"
echo '$USER'
echo $USER$PATH
echo "$USER"$PATH
echo "$USER $PATH"
echo $?
"echo $PATH" (moete dit in twee tokens?)
echo $NONEXISTENT_VAR
export VAR="ls -la"
$VAR

# --- COMPLEX EDGE CASES ---
echo "'$USER'"
echo '"$USER"'
cat<<EOF
ls|grep "a b c">file
"  "
"|"
">"
'|'
echo$USER (Should not expand unless space or valid var name)
$USER_ (Check if underscore is handled in var names)

# --- SYNTAX ERROR TESTING (INDEXING SHOULD NOT CRASH) ---
|
ls ||
ls | |
>
>>
<
<<
ls > >
"quote without end
'quote without end
